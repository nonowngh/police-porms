<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="IF_EX_API_GIS_05">

    <delete id="delete">
        DELETE FROM GD_GIS_VISIT_CATEGORYCODE2
    </delete>

    <insert id="insert" parameterType="java.util.List">
    INSERT INTO GD_GIS_VISIT_CATEGORYCODE2 (
				code,
				name,
				rnum,
				FRST_RGTR_ID,
				FRST_REG_DT,
				LAST_MDFR_ID,
				LAST_MDFCN_DT
    ) VALUES 
    <foreach collection="list" item="item" separator=",">
    (
        #{item.code},
        #{item.name},
        #{item.rnum},
        'indigo',
        NOW(),
        'indigo',
        NOW()        
    )
    </foreach>
  </insert>

</mapper>